import { BaseAction } from '../../../common/core/state-management/actions/base.action';

export function portalReducer(state = [], action: BaseAction) {
  switch (action.type) {
    default:
      return state;
  }
}






